services:
  - type: web
    name: email-receipt-reader
    env: node
    buildCommand: npm run build-all
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 